import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.watchduty.capmessagebugrepro',
  appName: 'capacitor-message-bug-repro',
  webDir: 'dist'
};

export default config;
